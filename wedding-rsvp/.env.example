# Server-side only — NOT exposed to browser
# API_MODE=mock  → use built-in mock data (no backend needed)
# API_MODE=real  → proxy to real backend at API_BASE_URL

API_BASE_URL=https://api.yourbackend.com
API_MODE=mock
